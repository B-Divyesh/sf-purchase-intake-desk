# M1 claim tests

The M1 builder adds one Playwright test tagged `@claim:<id>` for every entry in
`.factory/claims.json`. This planning work order does not add skipped or fake
claim tests: no product behavior exists to verify yet.
